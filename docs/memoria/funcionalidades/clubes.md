# Clubes

Última revisión: 2026-08-17

## Propósito

Gestionar clubes, membresías, administración, galería, desafíos y partidos entre clubes.

## Flujos actuales

La tab Clubes consulta `getMyClubs`: si hay membresías, muestra embebido el primer club por fecha de ingreso; si no, abre el explorador. No navega entre esas dos vistas, por lo que el detalle embebido ofrece volver al explorador. Creación agrega al fundador como administrador; solicitudes e invitaciones se resuelven en `club_join_requests`, cuyo trigger crea la membresía aprobada. Administra miembros, fotos, desafíos y la publicación de partido asociado.

## Reglas y permisos

Una persona puede integrar hasta tres clubes y una sola vez cada club. Estándar admite 15 integrantes/1 administrador y Premium 26/3; triggers validan topes. Sólo administradores pueden gestionar club, miembros, fotos, desafíos o mensajes importantes; miembros leen y escriben el chat. Un desafío pendiente por par de clubes y la expiración a siete días se controlan en base de datos.

## Ciclo formal de desafíos (en construcción)

La migración 41 abre el ciclo formal: trece estados con transiciones autorizadas, plazos de negociación y prórroga, cupos por club, método de inscripción y la guarda de base de datos contra desafiar a un club propio. Las reglas viven en `src/services/clubChallengeRules.js`, con `desafio_reglas()` como espejo en PostgreSQL. Ver [Reglas de negocio](../producto/reglas-de-negocio.md).

La 42 agrega el chat grupal `challenge:<id>` con todos los administradores de ambos clubes y la RPC `aceptar_desafio()`, que abre el plazo de negociación con hora de servidor.

La 43 pone a correr ese tiempo. `procesar_vencimientos_desafios()` corre por `cron` cada cinco minutos y aplica, sobre cada fila y con el estado esperado en el `where`: pendiente de más de siete días a expirado, negociación vencida a prórroga de 24 h, prórroga vencida sin dos «Sí» a sin acuerdo, y el paso del partido publicado a en juego y a esperando resultado. `refrescar_desafio()` hace lo mismo sobre una sola fila para que la pantalla no espere al cron. Responde la prórroga un administrador por club —`club_challenge_extension_replies` con `unique (challenge_id, club_id)`—, un «No» cierra el desafío en el acto y dos «Sí» reabren la negociación borrando las respuestas de la prórroga cerrada. La propuesta oficial vive en `club_challenge_proposals`, es idempotente por `client_token`, admite una sola pendiente por desafío y la puede leer cualquier integrante de los dos clubes, no sólo los administradores.

La 43c exige que la propuesta traiga la cancha ubicada en el mapa: `matches.latitud` y `longitud` son NOT NULL, así que una propuesta sin coordenadas nacía imposible de aprobar. `ClubProposalScreen` usa `LocationAutocomplete` —el mismo buscador de `PublishMatchScreen`— y las coordenadas se validan otra vez en el servidor, por tipo y por rango. La 43d cierra en `rechazar_propuesta()` el mismo hueco de doble pertenencia que ya cubría la aprobación.

La 44 cierra el ciclo hasta la publicación. `aprobar_propuesta()` verifica, crea el partido y avisa dentro de una sola transacción: exige ser administrador de un club del desafío distinto al proponente **y** no pertenecer al club proponente en ningún rol, crea el `matches` con `cupos_totales = 2 × cupos_por_club`, pasa el desafío a `publicado` y avisa a todos los integrantes de los dos clubes con `club_match_published`. Volver a pulsar aprobar devuelve el mismo partido en vez de crear otro. El botón de aprobar vive en `ClubProposalScreen`, no en la barra del chat: «Revisar propuesta» lleva allí, porque publicar un partido sin haber leído cancha, hora, cupos y cuota sería demasiado fácil de pulsar por accidente.

El partido entre clubes es privado hasta que termina: sólo lo ven los integrantes de cualquiera de los dos clubes, con o sin rol de administrador, y para ellos aparece en Inicio, Partidos y mapa, con dirección exacta y «Cómo llegar». Para todos los demás no existe. Al finalizar, lo único público es el resultado en el historial del club: clubes, día, marcador y V/E/D. Ver [Seguridad y privacidad](../arquitectura/seguridad-y-privacidad.md).

El partido publicado tiene tarjeta propia (`ClubMatchCard`): borde y halo verdes, franja «PARTIDO DE CLUBES», escudos y nombres de los dos clubes con VS, la fecha con más peso y CTA «Ver partido». Aparece en Partidos, en el detalle y en una sección propia de Inicio, «Próximo partido de tu club», que es la primera de la pantalla y sólo la ven los integrantes de alguno de los dos clubes; `seleccionInicio()` decide destacado y resto a la vez para que el mismo partido no salga dos veces. Los cupos NO se muestran como compartidos: «9 cupos para tu club» a los integrantes y «9 cupos por club» al resto, siempre desde `cupos_por_club`.

U3 está cerrada y sus migraciones 44e y 45 quedaron aplicadas en producción el 2026-08-13. `ClubMatchRosterScreen` muestra las dos nóminas, «X de Y inscritos de tu club», inscripción por llegada o postulación, confirmación/rechazo por un administrador del club del jugador, salida y retiro de postulación. Se refresca con Realtime para INSERT/UPDATE y sondeo de respaldo para DELETE/reconexiones. Proponer y aprobar preguntan por una reserva voluntaria con «No» predeterminado; la aprobación presenta un resumen final. Una reserva impedida no aborta la publicación y genera `club_match_reserva_omitida` sólo para la persona afectada. Las pruebas automatizadas y de esquema están completas.

La comprobación manual autenticada se hizo el 2026-08-13 con dos sesiones simultáneas y pasó: ambas cuentas ven las dos nóminas con los `username` reales y el conteo correcto por club; salir libera únicamente el cupo del club de quien se va y la otra sesión se actualiza sola por el sondeo de respaldo, sin recargar; inscribirse dos veces devuelve «Ya estabas en la nómina» y deja una sola fila. Los permisos de administración están cubiertos por el servidor y no por la interfaz: en un partido `orden_llegada` nadie queda `pendiente`, así que los botones de confirmar/rechazar no se dibujan, y quien lo demuestra es `45_inscripcion_por_club_test.sql` (caso 9: el administrador del club rival no puede confirmar; caso 11: nadie se confirma a sí mismo). El corte de 720 px se comprobó en Chrome a 390 × 845: las dos nóminas se apilan sin cortes ni superposiciones, los conteos y los `username` se leen completos y el botón de acción queda alcanzable bajo las listas. **U3 está cerrada desde el 2026-08-13.** Lo único que no tiene evidencia es el render nativo en un dispositivo físico; lo verificado es web a 904 px y a 390 px.

La interfaz de esta pantalla se acepta como funcional pero **no definitiva**: se priorizan lógica, permisos, estados y flujos, y el rediseño visual es un trabajo posterior y explícito. Ver [la decisión del 2026-08-13](../decisiones/2026-08-13-funcionalidad-antes-que-rediseno.md).

La consulta de la nómina se arma en `src/utils/nominaQuery.js` y sus columnas se contrastan con `supabase/schema.sql` y las migraciones en una prueba. Es la corrección de un fallo real: la nómina pedía `profiles.nombre`, columna que no existe, y PostgREST rechazaba la consulta entera con 400 y `42703`. El nombre visible sale de `username`, que es el único identificador de persona en `profiles`. Además, `getNominaPartido()` ya no traduce un fallo de esquema a lista vacía: devuelve `data: null` con el error, y `ClubMatchRosterScreen` distingue «no hay nadie inscrito» de «no se pudo cargar» —en el segundo caso no dibuja conteos, listas ni botón de acción, sólo el aviso y «Reintentar»—, porque un «0 de 7» con las listas en blanco y «Inscribirme» a quien ya estaba dentro se ve igual que un partido vacío.

U4.4 —cambios negociados— quedó desplegada el 2026-08-13 con la migración 46. Publicado el partido, ningún club lo edita solo: un administrador pide el cambio desde el hilo de negociación (`ClubMatchChangeScreen`, con el valor actual al lado del propuesto y sólo viajando lo que de verdad cambia) y un administrador del club CONTRARIO lo acepta o lo rechaza desde `CambioPartidoCard`. Mientras la solicitud está pendiente el partido conserva sus valores. Se negocian hora, cancha y cuota; el plazo es de 2 horas antes del encuentro y lo calcula PostgreSQL, mirándolo al pedir y otra vez al responder. Quien propone no responde lo suyo, ni otro administrador de su club, ni quien pertenezca al club proponente en cualquier rol. Rechazar admite un motivo opcional y queda registrado en el chat con el club y el `username` del administrador. Las reglas viven en `src/utils/cambioPartido.js` y los argumentos de las RPC en `cambioRpc.js`, los dos puros y probados; los componentes no deciden nada. El hilo se refresca solo con `crearSondeo` cada 15 segundos: `club_match_changes` y `club_challenge_events` no están en Realtime, así que sin eso la sesión de quien pedía el cambio seguía viendo «pendiente» después de que el rival respondiera. A quien pidió el cambio se le dice «Esperando la respuesta de X», no la razón interna por la que no tiene botones. Los NOMBRES de los dos clubes salen del partido pasado por `withClubs()`, nunca de la fila del desafío: ni `getChallenge` (`select('*')`) ni `refrescar_desafio` (devuelve `club_challenges` a secas) traen los clubes embebidos, y derivarlos de ahí daba siempre vacío. Es además la misma fila con la que `accionesDeCambio()` decide los botones, así que texto y permisos no pueden discrepar. **U4.4 está cerrada desde el 2026-08-14**, con la comprobación manual aprobada: dos sesiones a la vez, solicitud de cuota, el proponente lee «Pediste un cambio» y «Esperando la respuesta de chatgpt2», el rival la ve aparecer sola con «chatgpt pide un cambio» y los dos botones, rechaza, y la tarjeta desaparece sola en la otra sesión con su evento en el chat. El partido conservó sus valores. Lo único sin evidencia sigue siendo el render nativo en un dispositivo físico: todo lo comprobado es web.

La Tarea 5.1 —cancelación del encuentro y sanción del club— tiene la migración 47 **aplicada en producción el 2026-08-14**, con el arnés `47_cancelacion_y_sancion_test.sql` en 25/25 contra el esquema ya aplicado, la 47b cerrando el `EXECUTE` de su función de trigger y `send-push` redesplegada en su versión 7. La comprobación manual con las dos cuentas se hizo y **encontró un fallo de interfaz**, ya corregido: en el hilo de un encuentro cancelado, la barra sobre el compositor mostraba «Club sancionado» con el motivo de una sanción ANTERIOR del club —por otro encuentro— justo donde se lee el motivo de esta cancelación. El servidor tenía el dato bien; el fallo era el orden de `getChallengeCta`, que preguntaba por la sanción antes de mirar si el desafío ya estaba cerrado. **El estado cerrado manda sobre la sanción**: en un desafío cerrado no hay ninguna acción que la sanción pueda impedir, así que se muestra su propio estado, y donde la sanción sí bloquea algo su motivo nunca va suelto —lleva delante «Tu club está sancionado:», porque un motivo sin sujeto se lee como el del hilo en el que está pintado—. **U5.1 está cerrada desde el 2026-08-14**, con la comprobación manual aprobada en dos pasadas y dos sesiones simultáneas. La primera verificó el fondo: cancelar dentro de las 2 horas sancionó al club 14 días, que no pudo enviar ni recibir desafíos mientras su partido ya publicado seguía activo; cancelar con anticipación avisó de que no habría sanción y no dejó ninguna; el motivo quedó en el evento del chat y en el detalle del partido, con la nómina disponible; y el Trust Score de las dos cuentas se quedó en 100. La segunda confirmó el arreglo: la zona inferior del hilo cancelado dice «Cancelado» y ya no repite el motivo de la otra sanción, la advertencia de la sanción del club sigue visible arriba y separada, enviar un desafío con el club sancionado se rechaza con «Tu club está sancionado hasta el 28/08/2026 y no puede enviar desafíos», y el hilo de control conserva su propio motivo y su evento de sanción. Lo único sin evidencia sigue siendo el render nativo en un dispositivo físico: todo lo comprobado es web. Un administrador de cualquiera de los dos clubes cancela el encuentro publicado sin aprobación del rival, desde una barra que vive ARRIBA del hilo y no en el menú de tres puntos: cancelar se busca con urgencia y casi siempre con el partido cerca, y enterrarlo hace que se resuelva por WhatsApp mientras el club rival sigue organizando gente. El motivo es OBLIGATORIO —`club_sanctions.motivo` es `not null` con `check (length(trim(motivo)) > 0)` y la RPC rechaza el motivo en blanco antes de tocar nada—, queda en `matches.motivo_cancelacion`, en `club_challenges.motivo_cierre` y en el evento del hilo, y lo leen el club rival y los jugadores inscritos. El partido NO se borra: cambia a `cancelado` conservando chat, nómina e historial, reutilizando la lógica de la 34 pero **no** su función, porque `cancel_match()` descuenta Trust Score al organizador y en un partido de clubes ése es sólo el administrador que un día aprobó. Con más de 2 horas de anticipación no hay sanción; dentro de las 2 horas previas —y también con el partido ya empezado, que da diferencia negativa— el CLUB queda 14 días sin poder crear, aceptar, proponer ni aprobar desafíos nuevos, y **ninguna persona pierde un solo punto de Trust Score**. Quien administra los dos clubes no puede cancelar: la sanción recae sobre un club concreto y no hay forma de decidir cuál, el mismo conflicto de doble pertenencia que cierran la 43d y la 46. Los partidos que el club sancionado ya tenía publicados se conservan y se juegan (decisión C3 del plan): cancelarlos castigaría al club rival y a los inscritos, que no hicieron nada. Las reglas del cliente viven en `src/utils/cancelacionEncuentro.js`, puro y probado, y `CancelarEncuentroBar` no decide nada; la advertencia de la sanción se lee ANTES de confirmar, y sin hora legible se advierte igual, porque prometer «sin sanción» y que la haya es peor que lo contrario. La Tarea 5.2 —incomparecencia y revisión— tiene la migración 47c **aplicada en producción el 2026-08-15**, con los tres arneses en verde contra el esquema ya aplicado (47c 26/26, 47 25/25, 47b 5/5) y `send-push` en su versión 8. **U5.2 está cerrada desde el 2026-08-17**, con la comprobación manual de dos cuentas aprobada sobre `90e8f92`: las dos solicitudes de revisión, el congelamiento, la resolución intermedia y la restauración final se propagaron solas dentro del sondeo de 15 segundos, sin recargar ninguna sesión. El desafío volvió a `en_juego`, las dos sanciones quedaron `retirada` sin borrarse, no quedaron sanciones activas ni revisiones pendientes, y hay dos eventos y dos avisos de resolución sin duplicados. Con la 5.1 cerrada el 2026-08-14, **la Fase 5 queda cerrada**. Lo único sin evidencia sigue siendo el render nativo en un dispositivo físico: todo lo comprobado es web. La primera pasada encontró un fallo real de sincronización entre sesiones, corregido antes de cerrar: el hilo leía las sanciones, los informes y las revisiones sólo al montar y después de una acción PROPIA, así que tras la acusación cruzada el club acusado no veía «Solicitar revisión» hasta recargar la página. La premisa venía de la 47 —una sanción sobre mi club sólo podía nacer de que mi propio club cancelara algo, y por eso estaba excluida del sondeo «a propósito»— y la 47c la invirtió: informar deja la sanción sobre el club CONTRARIO, pedir la revisión congela el desafío para los dos, y **la resolución no la hace ninguna de las dos sesiones**, la ejecuta `service_role` desde el panel. Ahora el expediente entero entra en el mismo sondeo de 15 segundos que ya cubría la bitácora y los cambios de partido, junto con la fila del desafío, que se relee con `getChallenge` y no con `refrescar_desafio` porque esa RPC escribe. Todo lo que se refresca pasa por `refrescarDesafio`, incluidas las acciones propias, para que no queden dos caminos para el mismo dato. Las tres lecturas viven en `src/utils/expedienteSancion.js`, que recibe el cliente por parámetro —`clubSanctions.js` sólo le ata el de Supabase—, y eso es lo que permite que `expedienteSancion.test.js` recorra los cargadores reales y el sondeo real con dos sesiones y la RLS de la 47c aplicada a mano, en vez de pasarle la sanción a la función que decide el botón. Una rebanada que no se pudo leer conserva la que ya estaba en vez de vaciarse —si no, cada corte de red apagaría el botón— y sin novedades el expediente devuelve el mismo objeto para que la barra no se recalcule sola cada quince segundos. Cuando el estado del desafío cambia se vuelve a pedir `getThreadAccess`, que es lo que deja el compositor de solo lectura mientras dura el congelado. Informar una incomparecencia sólo se puede desde la hora del partido y hasta 24 horas después: el borde de abajo impide «informar» que alguien faltó a un partido que no ha empezado, y el de arriba cierra la denuncia tardía, que es la que no se puede comprobar. Informa un administrador contra el OTRO club, uno por partido y por club acusado, así que los dos pueden acusarse y cada acusación se revisa por separado. La sanción nace `provisional`: bloquea desde el primer momento —si esperara a la revisión no serviría de nada— pero se llama así porque todavía no la miró nadie, y el aviso lo dice. **Informar no congela nada**; el desafío pasa a `bloqueado_sancion` cuando alguien pide la revisión y vuelve a su estado exacto cuando se resuelve la última pendiente, con cualquiera de las dos decisiones. Mientras tanto el hilo queda de solo lectura —`bloqueado_sancion` no está en los `estados_activos` que mira `chat_puede_escribir_desafio()`— y la conversación se conserva entera. Retirar la sanción no borra su fila: la marca `retirada`. **La resolución no tiene interfaz y no es un olvido**: `resolver_revision_sancion` sólo la ejecuta `service_role` desde el panel de Supabase, y eso está anotado como P1 en [Pendientes](../operacion/pendientes.md) con la consulta de la cola. **Quién puede responder un reto lo decide una sola regla**, `src/utils/permisosDesafio.js`, y las dos pantallas que lo preguntan usan esa. Antes cada una lo resolvía por su cuenta y las dos se equivocaban con una cuenta que administra VARIOS clubes: «Avisos» comparaba `getMyClub()` —el primer club por `joined_at`— contra `clubRetadoId`, así que sólo el club más antiguo mostraba botones; y «Desafíos» deducía el rol de `listMembers(clubId)` sin distinguir «no soy admin» de «no pude averiguarlo», porque descartaba el `error` del servicio y un usuario nulo daba el mismo resultado. Lo encontró la comprobación manual de la 5.2: el aviso de un reto nuevo llegaba sin acciones mientras uno viejo —dirigido justamente al primer club— sí las conservaba. Ahora `getMisClubesAdmin()` trae todas las membresías administrativas en una consulta y devuelve `null` cuando falla, que **no** es lo mismo que `[]`: la bandeja lo dice en pantalla en vez de esconder los botones en silencio. Las dos secciones de la bandeja no se deciden igual —en «Recibidos» manda el club retado y en «Enviados» el retador—, y eso también está probado. U3 se desplegó en el orden 44e y después 45. El plan por fases está en `docs/superpowers/plans/2026-08-10-desafios-clubes.md`. El flujo antiguo sigue vivo: los desafíos en estado `aceptado` conservan su conversación directa entre dos administradores, permitida por `chat_valid_club_challenge_dm()`.

La Tarea 6.1 —resultado del encuentro— tiene las migraciones 48 y 48b **aplicadas en producción el 2026-08-17**, con `48_resultado_test.sql` en 19/19 contra el esquema ya aplicado, corrido dos veces (antes y después de la corrección 48b). Terminado el partido (`club_challenges.estado = 'esperando_resultado'`), un administrador de cualquiera de los dos clubes propone el marcador desde `ClubResultScreen` y marca quién llegó de verdad sobre la nómina ya cargada por U3: cada inscrito parte tildado como que asistió, y `proponer_resultado()` traduce lo que quede destildado a `no_asistio` sobre `attendees` (el resto, a `confirmado_gps`), reutilizando ese vocabulario en vez de inventar uno nuevo. Sólo un administrador del club CONTRARIO al proponente confirma o rechaza; aceptar cierra el desafío en `finalizado` y publica `matches.estado = 'finalizado'`, rechazar lo deja en `resultado_en_disputa` sin tocar ninguna estadística. `club_record()` e `historial_publico_club()` sólo cuentan `club_match_results.estado = 'confirmado'` — un resultado propuesto o rechazado no mueve el récord de nadie, y el historial público ya no devuelve el marcador nulo que la 44d dejaba pendiente. **Corregido el mismo día en la 48b**: la 48 dejaba a un club reabrir su propia disputa proponiendo de nuevo, contradiciendo la regla que ya vivía en `clubChallengeRules.js` («sólo la moderación puede reabrir una disputa; nunca se cierra sola») y en `getChallengeCta()`, que siempre mostró ese estado deshabilitado. `resultado_en_disputa` queda como un callejón sin salida para el club, del mismo tipo que `bloqueado_sancion` — ver [Pendientes](../operacion/pendientes.md). `ChatThreadScreen` ahora también carga el resultado activo del desafío —antes nunca se pedía, así que el CTA no podía distinguir «Registrar resultado» de «Confirmar resultado»— y se lo pasa a `challengeCtaContext()`. La Tarea 6.3 (memoria) del plan sigue sin cerrar.

La Tarea 6.2 —historial real y apagado de los fixtures— tiene la migración 49 **aplicada en producción el 2026-08-17**, con `49_historial_test.sql` en 13/13 contra el esquema ya aplicado. El perfil del club ya no muestra nada inventado: el historial sale de `historial_club()` y las estadísticas de `club_estadisticas()`, y los tres partidos de ejemplo («Deportivo Ñuñoa 1-0», «Atlético Maipú 2-3», «Los Cóndores 2-2») con su récord 1-1-1 se borraron junto al interruptor `DEMO_HISTORIAL` que los encendía en `__DEV__`. Con ellos se fue también el placeholder de la galería, que colgaba del mismo interruptor. **`historial_publico_club()` no se tocó**: su LEFT JOIN publica cualquier partido `finalizado` aunque nadie haya confirmado el marcador —y eso pasa de verdad, porque `save_match_attendance()` de la 33 pone `finalizado` sin mirar `club_match_results`—, así que la tarjeta lo pintaba como partido jugado con un «vs» donde va el score. `historial_club()` hace ese join INTERNO: sin resultado `confirmado` el encuentro no aparece. La proyección de la 44d se conserva tal cual porque su forma es el contrato que comprueba `44d_partido_privado_test.sql` (caso 15), y el caso 5 del arnés nuevo comprueba justamente que las dos sigan difiriendo. El MARCADOR SE LEE DESDE EL CLUB QUE SE MIRA, no desde el local: la misma fila «Club A 3-1 Club B» es «Victoria 3-1» en el perfil de A y «Derrota 1-3» en el de B, y quién fue local se dice aparte, en la línea de contexto de la tarjeta. La letra V/E/D se deriva de los dos números que la tarjeta pinta —no de la columna `resultado`— para que la insignia no pueda contradecir al marcador; una prueba comprueba que la derivación y el servidor coincidan en los cinco casos y desde los cuatro lados. `club_estadisticas()` envuelve `club_record()` en vez de copiar su `case` y agrega PJ, GF y GC, que el cliente no puede calcular sin mentir porque el historial viaja paginado; `getClubRecord()` se retiró de `clubResults.js` para no dejar dos caminos de cliente al mismo dato. La lógica vive en `src/utils/historialClub.js`, que recibe el cliente por parámetro —`clubMatches.js` sólo le ata el de Supabase—, y ahí se corrigió un corrimiento de día real: `new Date('2026-07-28')` es medianoche UTC y en Chile eso es el 27, así que la fecha se parte a mano. Hoy `club_match_results` está vacía en producción, de modo que todos los perfiles muestran el estado vacío —«Aún no hay partidos en el historial»— y quien demuestra el caso poblado es el arnés SQL.

## Pantallas y dependencias

- Pantallas: `ClubsScreen`, `ClubDetailScreen`, `ExploreClubsScreen`, creación/edición, miembros, galería, invitación, planes, desafíos, `ClubProposalScreen`, `ClubMatchRosterScreen` y `ClubResultScreen`.
- Código: `src/services/clubs.js`, `clubGallery.js`, `clubChallenges.js`, `clubProposals.js`, `clubRoster.js`, `clubMatches.js`, `clubResults.js`, `clubChallengeRules.js`, `clubMatchRules.js`, `src/utils/rivalClubsQuery.js`, `src/utils/nominaQuery.js`, `src/utils/challengeThread.js`, `src/utils/resultadoRpc.js`, `src/utils/historialClub.js`, `src/components/club/` y `src/components/clubes/`.
- Backend: tablas de clubes, fotos, desafíos, partidos y notificaciones de migraciones 11, 24 a 29 y 41 a 49; 44e/45/47/47c/48/48b/49 están aplicadas.

## Estados, errores y problemas conocidos

La tab mantiene un loader hasta resolver membresía y refresca al aceptar invitación. Si falta `clubs.modalidad` (migración 29), el servicio reintenta sin ella y la UI muestra modalidad sin definir. La disponibilidad de topes y RLS reales exige migraciones aplicadas en Supabase.

## Notas relacionadas

- [Base de datos](../arquitectura/base-de-datos.md)
- [Chat](chat.md)
- [Avisos y push](avisos-y-push.md)
