// En web no mostramos mapa: react-native-maps no funciona y queremos
// preservar la experiencia original de la pestaña Buscar (lista + filtros).
export default function MatchMap() {
  return null;
}
